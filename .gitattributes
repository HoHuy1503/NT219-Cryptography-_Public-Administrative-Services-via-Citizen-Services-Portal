* text=auto eol=lf

# Keep generated or evidence artifacts byte-for-byte.
*.enc -text -diff -merge
*.bin -text -diff -merge
*.pcap -text -diff -merge

# Certificates are tracked as text, but should still stay LF-normalized.
*.crt text eol=lf
